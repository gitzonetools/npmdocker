image: hosttoday/ht-docker-dbase:npmts

stages:
- test
- release

test:
  stage: test
  script:
    - npm install
    - npm test
  tags:
    - docker
    - lossless
    - priv

release:
  stage: release
  environment: npmjs-com_registry
  script:
    - npmci publish
  only:
    - tags
  tags:
    - docker
